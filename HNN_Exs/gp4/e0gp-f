# Zmod4xZmod4 gen by a, b with a first
_RWS := rec(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a, A, b,B],
  inverses := [A, a, B,b],
  equations := [
  [a*a*a*a, IdWord ],
  [b*b*b*b, IdWord],
  [b*a,a*b]
  ]

);
