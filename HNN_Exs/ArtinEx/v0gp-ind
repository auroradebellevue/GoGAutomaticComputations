# Artin 3,3,3 gen by a, b, c 
_RWS := rec(
  isRWS := true,
  ordering := "shortlex",
  generatorOrder := [a, A, b, B, c, C],
  inverses := [A, a, B, b, C, c],
  equations := [
  [b*a*b, a*b*a ],
  [c*a*c, a*c*a ],
  [c*b*c, b*c*b ]
  ]
);
